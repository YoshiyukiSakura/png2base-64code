# png2base-64code
a php application converts png files to css code
# usage
make a directory in the directory where you put shell.php, and move png file in it.  
make sure you put png file in it only, the script doesn't have Robust currently.  
run ```php shell.php```  
you have you icon.css now    
